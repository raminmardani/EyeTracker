# === AIRE SDLC Agentic Framework — auto-managed (do not edit by hand) ===
# Generated by 'aire init' / 'aire update'. Re-running these commands
# refreshes this block, preserving paths added by teammates on other IDEs.
# Edit entries outside the markers manually.
SPEC/agents/
SPEC/rulebooks/
SPEC/templates/
SPEC/workflows/
.claude/agents/
.claude/skills/
.claude/claude.md
# === end AIRE SDLC Agentic Framework ===

# === Project — maintained manually (outside the auto-managed block above) ===

# Virtual environments. The repo previously tracked a 752 MB macOS-built .venv
# (14,251 files); it is intentionally excluded now. Recreate locally instead.
.venv/
venv/
env/
ENV/

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class

# Packaging / build artifacts
build/
dist/
*.egg-info/
.eggs/

# Test, coverage and lint caches
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
*.swp

# AIRE parallel-wave runtime signals (created by aire-dev-implement)
docs/plans/.parallel/
